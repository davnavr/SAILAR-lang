namespace SAILARSharp.Core;

/// <summary>Builds the list of records in a SAILAR module.</summary>
public unsafe sealed class Builder {
    internal readonly struct Opaque { }

    private Opaque* builder;
}
